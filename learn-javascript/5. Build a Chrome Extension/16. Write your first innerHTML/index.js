// Use .innerHTML to render a Buy! button inside the div container
const divArea = document.getElementById("container")
// divArea.innerHTML = "<button>" + "Button" + "</button>"
divArea.innerHTML = "<button>Button!</button>"

